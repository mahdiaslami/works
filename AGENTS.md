# Works

This file contains coding instructions in various sections and they are required to follow them.

## Vue

- Always put `<script>` tag at the begining of the file

## UI Guideline

- Always use tailwindcss classes
- Only use these colors:
    - `sky` as the primary color
    - `slate` as the tint color
    - `green` as the active color